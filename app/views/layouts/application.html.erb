<!DOCTYPE html>
<html>
  <head>
    <title>Sigor</title>
    <%= csrf_meta_tags %>


  </head>

  <body>
    <p class="notice"><%= notice %></p>
     <p class="alert"><%= alert %></p>

     <% if user_signed_in? %>
      Estas logeao Como: <strong><%= current_user.email %></strong>.
      <%= link_to 'Editar Perfil', edit_user_registration_path, :class => 'navbar-link' %> |
      <%= link_to "Cerrar Sesion", destroy_user_session_path, method: :get, :class => 'navbar-link'  %>
      <% else %>
      <a href="/users/sign_in">Login</a>
     <a href="/users/sign_up">Sign Up</a>
      <% end %>
    <%= yield %>
  </body>
</html>
